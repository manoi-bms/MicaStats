<Project Sdk="Microsoft.NET.Sdk">
    <PropertyGroup>
        <TargetFramework>net8.0-windows</TargetFramework>
        <OutputType>WinExe</OutputType>
        <ApplicationIcon>icon.ico</ApplicationIcon>
        <Win32Icon>icon.ico</Win32Icon>
        <Authors>Chaiyaporn Suratemeekul (manoi-bms)</Authors>
        <Company>manoi-bms</Company>
        <Product>MicaStats</Product>
        <Description>Advanced Hardware Telemetry Overlay for Windows 11</Description>
        <Copyright>Copyright © 2026 Chaiyaporn Suratemeekul (manoi-bms)</Copyright>
        <UseWPF>true</UseWPF>
        <UseWindowsForms>true</UseWindowsForms>
        <DefaultLanguage>en-US</DefaultLanguage>
        <Version>1.2.0</Version>

        <!-- Project Options -->
        <Nullable>enable</Nullable>
        <LangVersion>latest</LangVersion>
        <ImplicitUsings>enable</ImplicitUsings>
        <SatelliteResourceLanguages>en-US</SatelliteResourceLanguages>
        <AssemblyName>MicaStats</AssemblyName>
        <RootNamespace>Kil0bitSystemMonitor</RootNamespace>

        <!-- App Options -->
        <ApplicationManifest>app.manifest</ApplicationManifest>
    </PropertyGroup>

    <ItemGroup>
        <None Include="icon.ico">
            <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
        </None>
    </ItemGroup>

    <!--
      This project lives at the repository root, so the SDK's default **/*.cs glob would compile
      the test project's sources into the application assembly. Exclude them explicitly.
    -->
    <ItemGroup>
        <Compile Remove="tests/**" />
        <None Remove="tests/**" />
        <Page Remove="tests/**" />
        <ApplicationDefinition Remove="tests/**" />
    </ItemGroup>

    <ItemGroup>
        <PackageReference Include="ModernWpfUI" Version="0.9.6" />
        <PackageReference Include="System.Diagnostics.PerformanceCounter" Version="8.0.0" />
        <PackageReference Include="System.Drawing.Common" Version="8.0.0" />
        <PackageReference Include="System.Management" Version="8.0.0" />
    </ItemGroup>

</Project>
